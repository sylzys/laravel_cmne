{{-- @if(can_impersonate())
@canBeImpersonated($entry, $guard = null)
    <a href="{{ route('impersonate', $entry->id) }}" class="btn btn-sm btn-link"><i class="la la-user"></i> Impersonate</a>
@endCanBeImpersonated
@endif --}}
